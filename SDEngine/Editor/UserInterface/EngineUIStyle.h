#pragma once
#include "Core/DataTypes/TypeDefenitions.h"

namespace EngineUIStyles {
	static const FColor DARK_BACKGROUND_COLOR = FColor::FromIntegerRGB(27, 27, 27);
	static const FColor BACKGROUND_COLOR = FColor::FromIntegerRGB(37, 37, 37);
	static const FColor LIGHT_BACKGROUND_COLOR = FColor::FromIntegerRGB(44, 44, 50);
	static const FColor SEPARATOR_COLOR = FColor::FromIntegerRGB(32, 32, 32);

	static const FColor DIM_ICON_TINT = FColor::FromIntegerRGB(123, 123, 123);

	static const FColor LIGHT_TEXT_COLOR(1.0f, 1.0f, 1.0f);
	static const FColor DARK_TEXT_COLOR(0.0f, 0.0f, 0.0f);
}
