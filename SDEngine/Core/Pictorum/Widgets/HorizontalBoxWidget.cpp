#include "HorizontalBoxWidget.h"

HorizontalBoxWidget::HorizontalBoxWidget(TString Name) : PictorumWidget(Name) {

}
void HorizontalBoxWidget::Tick(float DeltaTime, const FRenderGeometry& Geometry) {
	//float horizontalSize = Scale.x / (float)Children.Count();

	//for (int i = 0; i < Children.Count(); i++) {
	//	vec2 size = vec2(horizontalSize * i, Scale.y);
	//	Children[i]->GetScale() = size;
	//}
}